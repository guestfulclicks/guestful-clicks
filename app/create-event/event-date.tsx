import { View, Text } from 'react-native';

// TODO: Build event date picker screen
export default function EventDate() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Event Date — coming soon</Text>
    </View>
  );
}
