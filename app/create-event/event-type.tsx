import { View, Text } from 'react-native';

// TODO: Build event type selection screen
export default function EventType() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Event Type — coming soon</Text>
    </View>
  );
}
